export interface InterviewQuestion {
  question: string;
}

export interface InterviewSession {
  id: number;
  role: string;
  questions: InterviewQuestion[];
}export interface InterviewQuestion {
  question: string;
}

export interface InterviewSession {
  id: number;
  role: string;
  questions: InterviewQuestion[];
}